# 0.2.3

- `tp skills create --global` now installs skills where your agent looks for them
  (e.g. `~/.claude`, `~/.codex`).
- Clearer help when suggested timesheets look "missing" — usually there's just no CRM
  booking for that day.
